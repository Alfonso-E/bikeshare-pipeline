# Collection layer only. Pinned to what this was verified against 2026-08-20.
#
# Whatever you use downstream -- pandas, polars, dbt, sklearn -- is your call
# and belongs in its own requirements file, so the collector stays installable
# on its own and CI stays fast.

requests==2.34.2
PyYAML==6.0.3
pyarrow==25.0.1

# Used only to read the Parquet lake back for the integrity checks. Any reader
# works for your own analysis; this one is here because it needs no server.
duckdb==1.5.5

pytest==9.1.1
